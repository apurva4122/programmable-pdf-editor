/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Remove standalone for now - use regular npm start
    // output: 'standalone',
}

module.exports = nextConfig

